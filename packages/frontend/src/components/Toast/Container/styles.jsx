import styled from 'styled-components'

export const ToastContainer = styled.div`
	position: fixed;
	top: 0;
	right: 0;
	padding: 30px;
	overflow: hidden;

	display: flex;
	flex-direction: column;
	gap: 15px;
`
